package functions;
@FunctionalInterface
// it is an interface with only one abstract class it can have multiple main or functions
public interface Calculator{
    // the abstract method is operate using two variable a and b of integer type
    int operate(int a, int b);
}
